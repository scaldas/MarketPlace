define(['component/_carritoMasterComponent'],function(_CarritoMasterComponent) {
    App.Component.CarritoMasterComponent = _CarritoMasterComponent.extend({
		postInit: function(){
			//Escribir en este servicio las instrucciones que desea ejecutar al inicializar el componente
		}
    });

    return App.Component.CarritoMasterComponent;
});